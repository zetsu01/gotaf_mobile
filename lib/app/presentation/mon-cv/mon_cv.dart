import 'package:flutter/material.dart';

class MonCvPage extends StatelessWidget {
  const MonCvPage();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [],
      ),
    );
  }
}
